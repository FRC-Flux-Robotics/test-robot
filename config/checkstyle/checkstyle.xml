<?xml version="1.0"?>
<!DOCTYPE module PUBLIC
    "-//Checkstyle//DTD Checkstyle Configuration 1.3//EN"
    "https://checkstyle.org/dtds/configuration_1_3.dtd">

<!--
    Checkstyle configuration for FRC Team 10413 FLUX Robotics.
    Relaxed ruleset suitable for FRC development patterns.

    Rules focus on catching real issues without being overly strict
    about style preferences that vary in FRC codebases.
-->
<module name="Checker">
    <property name="severity" value="error"/>
    <property name="fileExtensions" value="java"/>

    <!-- Exclude generated code (TunerConstants, etc.) -->
    <module name="BeforeExecutionExclusionFileFilter">
        <property name="fileNamePattern" value=".*[\\/]generated[\\/].*\.java$"/>
    </module>

    <!-- File length check (relaxed for FRC) -->
    <module name="FileLength">
        <property name="max" value="2000"/>
    </module>

    <!-- Line length (relaxed for FRC - many builder patterns) -->
    <module name="LineLength">
        <property name="max" value="150"/>
        <property name="ignorePattern" value="^package.*|^import.*|a href|href|http://|https://|ftp://"/>
    </module>

    <module name="TreeWalker">
        <!-- Import checks - allow star imports (common in FRC for Units.*) -->
        <module name="RedundantImport"/>
        <module name="UnusedImports"/>

        <!-- Braces - allow both styles (FRC uses both) -->
        <module name="NeedBraces">
            <property name="allowSingleLineStatement" value="true"/>
        </module>

        <!-- Block checks -->
        <module name="EmptyBlock">
            <property name="option" value="TEXT"/>
        </module>

        <!-- Coding - catch real bugs -->
        <module name="EmptyStatement"/>
        <module name="EqualsHashCode"/>
        <module name="SimplifyBooleanExpression"/>
        <module name="SimplifyBooleanReturn"/>
        <module name="StringLiteralEquality"/>
        <module name="DefaultComesLast"/>
        <module name="FallThrough"/>
        <module name="MissingSwitchDefault"/>

        <!-- Modifiers -->
        <module name="ModifierOrder"/>
        <module name="RedundantModifier"/>

        <!-- Misc -->
        <module name="ArrayTypeStyle"/>
        <module name="UpperEll"/>
        <module name="OuterTypeFilename"/>
    </module>
</module>
